<?xml version="1.0" encoding="UTF-8"?>
<plan name="Machine-learned responses">
	<!-- "timeout" for a stage is an absolute number of seconds that elapses 
	(from the script's beginning) before that stage starts.  -->
	<!-- "delay" for a stage is the number seconds that elapses in-between stages.  -->
	
    <stage name="initiate" type="other" delay="0">
        <step type="prompt" delay="600">Try typing "Teachers are amazing" for a response of "Detected," or try typing "No reason" for a response of "NOT Detected."|Also try other entries to evoke these responses. Most won't but some will.</step>
		<step type="prompt" delay="30">Logging out in about 30 seconds ...</step>        
        <step type="logout" delay="0">LOGOUT</step>
    </stage>
</plan>