# Path to the process file relative to the agent's runtime directory 
#   (or the agent's directory when deployed on a server).
#   Path should include the ending directory delimiter ("/" or "\").  
process_path=processes/
